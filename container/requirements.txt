# torch is installed as a separate Dockerfile layer from the CUDA wheel index.
# Do NOT add torch here - it would pull in the CPU-only PyPI wheel.
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0
numpy>=1.24.0
networkx>=3.0
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0
