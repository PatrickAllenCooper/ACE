# ACE Paper: AISTATS 2027 Submission - Improvement Roadmap

**Updated:** September 7, 2026

**RESOLVED (was BLOCKING):** the total-query-matched ("budget-fairness")
rerun has now been executed at BOTH scales and ACE wins both times -- 76% at
5 nodes, 43-50% at 30 nodes. This was the experiment that resolved the
reviewers' central objection, and it is now reported in the paper's Budget
Accounting section, abstract, and Limitations. Table 1 remains episode-matched
by design (stated in its caption); the query-matched comparison is reported
alongside it rather than replacing it.

**NEW BLOCKER:** the 30-node ACE seed expansion (seeds 2022-2026) does not
replicate the headline 1.95 result -- the new seeds land at the passive-
baseline plateau (5.685 +/- 0.687) with visibly pathological DPO training.
Do NOT report N=8 for Table `tab:large-scale` until this is resolved. See
current_status.txt for the full diagnosis and the four options.

**VENUE:** retargeted to AISTATS 2027 (deadline Oct 6, 2026). The paper
directory is still named `paper/iclr_ace_2027/` and still uses the ICLR
style file; retemplating to AISTATS/PMLR format is outstanding.
**Prior status:** Submitted to NeurIPS 2026 (paper/neurips_ace_2026/), received
critical reviews. Older ICML 2026 review-response history has been moved to
status_archive/ICML2026_REVIEWER_RESPONSE_MAR2026.txt (superseded venue, kept
for reference only -- do not treat as current).
**Current status:** Comprehensive resubmission to ICLR 2027
(paper/iclr_ace_2027/), reframed around the LM-prior + lookahead-selection
finding rather than DPO.

## NeurIPS 2026 review concerns and how each is addressed

| Concern | Addressed by |
|---------|--------------|
| Budget accounting counts executed interventions only, not lookahead probes (4/5 reviewers) | Query counter tagging every `env.generate()` call; `--lookahead_on_student` (zero-oracle-query lookahead mode); `--query_budget` (total-query-matched runs); new "Budget Accounting" section with full multiplier audit (ACE 4-5x, Max-Variance ~65x, Bayesian OED ~500x samples/1001x calls, Random/Round-Robin/PPO 1x) |
| Is DPO specifically necessary, or would a simpler preference update work? (d6tT) | `--policy_update {dpo, sft_best, ranking}` implemented; paper reframed so a tie is not fatal to the contribution (title and abstract now center LM-prior + lookahead, not DPO) |
| Non-stationary reward assumption (Assumption 4.1) contradicted by the paper's own scale-invariance evidence (wZrW) | Recast as "local, slow-drift reward decomposition" (matches the Spearman rho > 0.85 adjacent-episode stability already reported); Proposition 4.2 demoted from load-bearing theory to motivating illustration |
| Diversity term and history window only defined "see the public code repository" | Full closed-form definitions (node importance Eq., diversity Eq.) added to the main text, with constants verified against the actual `ace_experiments.py` defaults (previous paper text had wrong constants -- see below) |
| Missing node-importance ablation alongside the existing no-diversity row (wZrW) | `no_node_importance` ablation (`--cov_bonus 0.0`) added to `run_ablations_fast.py`; CURC job scripts written; queued, not yet run |
| Bayesian OED baseline contradicts its own appendix's stated per-step cost when scaled to 30 nodes (F4Cb) | 30-node Bayesian OED job script exists (`run_30node_baseline_seed.py` / `curc_*` scripts); queued, not yet run |
| 30-node ACE only at N=3 seeds | Seed-expansion job scripts exist (target N=5-10); queued, not yet run |
| Missing DPT / in-context-exploration / goal-oriented BED related work | Added and citation-verified: Lee et al. 2023 (DPT, NeurIPS), Krishnamurthy et al. 2024 (in-context LLM exploration, NeurIPS), Zhang et al. 2025 (GO-CBED, arXiv:2507.07359) |

## Reward-constant discrepancy fixed this pass

The NeurIPS paper's Equation 4 and appendix stated `alpha=0.1` (node
importance) and `gamma=0.05` (diversity). Re-derived directly from the code
path exercised by `jobs/curc_ace_seed.sh` (which passes no reward-weight
flags, i.e. uses argparse defaults): there is no separate alpha multiplier --
`cov_bonus`=60.0 is the node-importance term's entire scale constant -- and
`diversity_reward_weight`=0.3, not 0.05. Corrected in
`paper/iclr_ace_2027/paper.tex`, `appendix.tex`, and the hyperparameter table.
The already-submitted NeurIPS version is left unedited.

## What is done vs. queued (do not conflate these)

DONE (this environment, no cluster needed):
- Query-counter instrumentation, `--lookahead_on_student`, `--query_budget`,
  `--policy_update` alternatives, and unit tests for all four.
- Full oracle-query-multiplier audit of every baseline (code-read, not rerun).
- `no_node_importance` ablation flag + job scripts.
- New `paper/iclr_ace_2027/` directory: retitled paper, reframed abstract and
  contributions, theory recast, reward terms defined in the main text,
  new Budget Accounting section, expanded related work, Reproducibility and
  Broader Impacts sections, corrected appendix.
- Citation verification (title/authors/venue/arXiv ID against primary
  sources) for the three newly added references.

COMPLETED SINCE (all synced to git under results/, Sept 7):
- Budget-fairness suite, 5-node AND 30-node: DONE. ACE wins at both scales.
  This was the highest-priority item and it is closed.
- Bayesian OED at 30 nodes, N=5 seeds: DONE (best 5.78 +/- 0.05), now a row
  in Table `tab:large-scale`; retires the reviewer objection that we asserted
  OED does not scale without demonstrating it.
- 100-node frontier and free-lookahead K-scaling (K in {4,8,16,32} x 3
  seeds): DONE, not yet folded into the paper's figures/tables.
- 30-node ACE seed expansion: RAN, but does not replicate -- see NEW BLOCKER
  above. Not folded into the paper.

STILL RUNNING (resubmitted Sept 7 after root-causing a HF 429 + OOM failure):
- DPO vs sft_best vs ranking on the 5-node benchmark, N=5 seeds
  (jobs 32208655-669).
- Node-importance ablation, 2 configs x 3 seeds (jobs 32208673-678).
- Model-scale sweep (`jobs/curc_submit_model_scale_sweep.sh`, Aug 2026 Alpine
  H200/RTX Pro 6000 expansion): zero-shot capability scaling across
  Qwen2.5 0.5B-32B, and a DPO-vs-no-DPO comparison at 0.5B/1.5B/3B. Secondary
  to the budget-fairness suite, not a decision gate.
- 100-node scaling frontier (`jobs/curc_submit_100node_frontier.sh`) and the
  free-lookahead K-scaling ablation (`jobs/curc_submit_k_scaling_student.sh`),
  both enabled by the same hardware expansion. Also secondary.

NOT DONE, blocked on tooling not available in this environment:
- Local LaTeX compilation (no pdflatex/bibtex/latexmk here; ICLR style/bst
  files were fetched from the official ICLR GitHub repo but never compiled
  locally). Compile once locally and fix any warnings before submission.
- Re-verification of the NeurIPS-round citations carried over into the ICLR
  bibliography (only the three new ones added this pass were checked).

## Recommended order of remaining work

1. Run the budget-fairness suite on CURC first -- it is the decision gate.
2. Aggregate with `scripts/analysis/aggregate_query_budget.py`; update
   Table 1 and the Budget Accounting section with real numbers.
3. Run the remaining queued experiments (OED@30, seed expansion,
   DPO-alternatives, node-importance ablation) and fold results into the
   relevant tables.
4. Time/GPU-allocation permitting, run the secondary model-scale sweep,
   100-node frontier, and free-lookahead K-scaling experiments; fold into a
   new subsection rather than displacing the primary results.
5. Full reference-by-reference citation check on the complete bibliography.
6. Local LaTeX compile, fix warnings, proofread, submit.
